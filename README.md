# Haven

Haven is an AI-powered platform connecting Christians experiencing
persecution with trusted help, encouragement, and a global community
of prayer.

## Running Haven Locally

Because Haven uses browser features that require a web origin,
do not open `index.html` directly with `file://`.

From the project directory, run:

python3 -m http.server 8000

Then open:

http://localhost:8000
